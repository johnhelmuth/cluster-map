{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://in-dire-straits.space/data/schemas/universe.schema.json",
  "title": "Universe",
  "description": "A dataset of all of the clusters of star systems in the universe in a Role Playing Game.",
  "type": "object",
  "properties": {
    "type": {
      "description": "The type of thingy the data describes, 'universe'.",
      "const": "universe"
    },
    "schemaVersion": {
      "description": "The version of the schema for the data.",
      "const": "1"
    },
    "id": {
      "description": "A unique identifier for this universe",
      "type": "string"
    },
    "name": {
      "description": "A name for this universe",
      "type": "string"
    },
    "currentClusterId": {
      "description": "The ID of the cluster that is currently being viewed.",
      "type": "string"
    },
    "clusters": {
      "description": "The list of clusters in the dataset.",
      "type": "array",
      "items": {
        "description": "A cluster in the dataset.",
        "$ref": "https://in-dire-straits.space/data/schemas/cluster.schema.json"
      }
    }
  },
  "required": [ "clusters" ]
}